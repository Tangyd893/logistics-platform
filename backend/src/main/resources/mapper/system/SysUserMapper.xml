<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.system.repository.SysUserRepository">

    <select id="findByUsername" resultType="com.logistics.system.domain.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = false
    </select>

    <select id="findByPhone" resultType="com.logistics.system.domain.entity.SysUser">
        SELECT * FROM sys_user WHERE phone = #{phone} AND deleted = false
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM sys_user WHERE username = #{username} AND deleted = false
    </select>

</mapper>
